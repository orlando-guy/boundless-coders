export type TableHeaders = {
    key: string;
    header: string
}

export type TableRows = {
    id: string;
    name: string;
    createdAt: string;
    updatedAt: string;
    issueAccount: string;
    stars: string;
    links: string;
}